{% extends 'base.html.twig' %}

{% block title %}Bienvenue{% endblock %}

{% block body %}
 <div class="container">
        <h2>Les derniers posts</h2>
        <div class="row">
            {% for post in posts %}
    {# {{ dump(post) }} #}
            <div class="col-md-4">
                <h3>{{ post.title }}</h3>
                <p><a href="{{ path('app_post_show', {id: post.id }) }}"><img src="{{ post.image }}" alt="image"></p></a>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
